spring.datasource.url=jdbc:postgresql://localhost:5432/lealtix_db
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
server.port=8080
# CORS Configuration
cors.allowed-origins=http://localhost:4200
# SENDGRID
sendgrid.email.from=kike2013edj@gmail.com
sendgrid.api-key=${SENDGRID_API_KEY}
sendgrid.templates.pre-registro=d-dcab354275e741af957dd8eaef2e1148
sendgrid.templates.welcome=d-2592da3d11404fcdafb5bdd78430ee32
# Expiracion de token
invitation.token.expiry-hours=48
# Dominio base para links de invitacion
invitation.base-url=http://localhost:4200
# Spring Security
spring.security.user.name=admin
spring.security.user.password=admin
spring.security.user.roles=ADMIN
# Stripe API
stripe.api.key=${STRIPE_API_KEY}
