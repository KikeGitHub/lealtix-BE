# Application properties (dev/profile 'dev')
# Use UTF-8 encoding
#
# NOTA IMPORTANTE DE SEGURIDAD:
# Cuando este perfil 'dev' está activo, TODAS las rutas /api/** son PUBLICAS (sin autenticación).
# Esto es para facilitar el testing en el ambiente de desarrollo (Render).
# En producción, usar un perfil diferente donde solo endpoints específicos sean públicos.

# PostgreSQL Connection (Render)
spring.datasource.url=jdbc:postgresql://dpg-d5et4n2li9vc73d6mbfg-a/lealtix_db_vmbz
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

# Driver
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
server.port=8080

# Swagger Configuration
springdoc.swagger-ui.tagsSorter=alpha

# CORS Configuration (production/dev origins)
cors.allowed-origins=https://admin.lealtix.com.mx,https://lealtix.com.mx,https://www.lealtix.com.mx,https://lealtix-service.onrender.com

# SENDGRID
sendgrid.email.from=contacto@lealtix.com.mx
sendgrid.api-key=${SENDGRID_API_KEY}
sendgrid.templates.pre-registro=d-117d901d7d4a4368b3b6da75a862c588
sendgrid.templates.welcome=d-d75cfbc1655d4c93ab26ab6d587f4cf7
sendgrid.templates.welcome-customer=d-b441904e4b8f4b7792ecded3948aadcf
sendgrid.templates.welcome-customer-no-campaign=d-947cbcd8bdad46159632bcca19cd32e7
sendgrid.templates.coupon-redemption=d-51c21ffce1f64152b91bcda1370fc7bc

# Expiracion de token
invitation.token.expiry-hours=120
# Dominio base para links de invitacion
invitation.base-url=https://lealtix.com.mx
# Dominio BE
lealtix.backend.url=https://lealtix-service.onrender.com
# Dominio Dashboard
lealtix.dashboard.url=https://admin.lealtix.com.mx

# Spring Security
spring.security.user.name=admin
spring.security.user.password=admin
spring.security.user.roles=ADMIN

# Stripe API
stripe.api.key=${STRIPE_API_KEY}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET}

# JWT
jwt.secret=${JWT_SECRET}

# Cloudinary
cloudinary.cloud_name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api_key=${CLOUDINARY_API_KEY}
cloudinary.api_secret=${CLOUDINARY_SECRET}
cloudinary.url=${CLOUDINARY_URL}

# DEBUG logging for security troubleshooting
logging.level.org.springframework.security=DEBUG
logging.level.com.lealtixservice.config.SecurityDebugFilter=DEBUG
